
  # Editable Roadmap Interface

  This is a code bundle for Editable Roadmap Interface. The original project is available at https://www.figma.com/design/nHTrluf6BdKQ6V6epE8ftT/Editable-Roadmap-Interface.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  